# OpenOCD config for STM32F4-Discovery (on-board ST-Link/V2, SWD)
# ST-Link firmware V2J14S0 only supports HLA mode (needs >= V2J24 for DAP direct)
source [find interface/stlink-hla.cfg]
transport select hla_swd
source [find target/stm32f4x.cfg]

# Slow down for HLA mode
adapter speed 2000
